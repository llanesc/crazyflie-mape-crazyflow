[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crazyflie-mape-crazyflow"
version = "0.1.0"
description = "Multi-agent pursuit-evasion with Crazyflies using LEAP-C MPC and Crazyflow simulation."
authors = [{ name = "Carlos Llanes" }]
readme = "README.md"
license = { text = "MIT" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    # Core environment/runtime imports from crazyflie_mape_crazyflow.envs and utils.
    "array-api-compat>=1.10.0",
    "crazyflow>=0.1.0b0",
    "drone-models>=0.1.0b0",
    "gymnasium>=1.1.0",
    "jax>=0.7.0",
    "mujoco>=3.3.0",
    "numpy>=2.0.0",
    "PyYAML>=6.0.0",
    "scipy>=1.15.0",
]

requires-python = ">=3.11,<3.14"

[project.optional-dependencies]
learning = [
    # MAPPO training/evaluation policy stack.
    "torch>=2.0.0",
    "skrl>=2.0.0",
]
mpc = [
    # LEAP-C/acados policy and planner stack.
    "acados_template>=0.5.1",
    "casadi>=3.7.0",
    "leap-c>=2025.0.0",
    "torch>=2.0.0",
]
training = [
    "crazyflie-mape-crazyflow[learning,mpc]",
]
eval = [
    "crazyflie-mape-crazyflow[training]",
    "imageio>=2.30.0",
    "opencv-python>=4.10.0",
    "pillow>=10.0.0",
    "tqdm>=4.66.0",
]
analysis = [
    "matplotlib>=3.8.0",
    "pandas>=2.0.0",
    "SciencePlots>=2.0.0",
    "tensorboard>=2.15.0",
]
gpu = ["jax[cuda12]"]
all = [
    "crazyflie-mape-crazyflow[analysis,eval,gpu,training]",
]
dev = [
    "crazyflie-mape-crazyflow[analysis,eval,training]",
    "drone-controllers>=0.1.0b0",
    "packaging>=23.0",
    "pytest>=8.0.0",
    "pytest-timeout>=2.0.0",
    "ruff>=0.4.0",
]

[project.urls]
Repository = "https://github.com/utiasDSL/crazyflie-mape-crazyflow"

[tool.setuptools.packages.find]
include = ["crazyflie_mape_crazyflow*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["unit", "integration", "render"]

[tool.ruff]
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "build",
    "dist",
    "external",
]
line-length = 100
indent-width = 4
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "D"]
ignore = ["D100", "D104"]
fixable = ["ALL"]

[tool.ruff.lint.isort]
split-on-trailing-comma = false

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.dependencies]
pip = "*"
ruff = "*"
python = ">=3.11,<3.14"

[tool.pixi.pypi-dependencies]
crazyflie-mape-crazyflow = { path = "./", editable = true }
crazyflow = { path = "./external/crazyflow", editable = true }
drone-models = { path = "./external/crazyflow/submodules/drone-models", editable = true }
drone-controllers = { path = "./external/crazyflow/submodules/drone-controllers", editable = true }
leap-c = { path = "./external/leap-c", editable = true }

[tool.pixi.feature.gpu.pypi-dependencies]
jax = { extras = ["cuda12"], version = "*" }

[tool.pixi.feature.tests.dependencies]
pytest = ">=8.0.0"
pytest-timeout = ">=2.0.0"

[tool.pixi.feature.tests.tasks]
test = { cmd = "pytest -v tests", description = "Run tests" }

[tool.pixi.environments]
default = { solve-group = "default" }
gpu = { features = ["gpu"], solve-group = "default" }
tests = { features = ["tests"], solve-group = "default" }
