#!/bin/bash
# Wrapper script to run main_executor with the correct Python environment
# Uses MAPE_PYTHON if set, otherwise uses python3 from PATH

PYTHON="${MAPE_PYTHON:-python3}"

exec "$PYTHON" -m multiagent_pursuit_evasion.multiagent_pursuit_evasion "$@"
